/**
 * API's main exporter.
 *
 * This is simple intermediate place for more readable imports.
 * @since 0.1.0
 */
import {fetchAllCategories, fetchImageByID, fetchImages} from "./api";

export {fetchAllCategories, fetchImages, fetchImageByID}